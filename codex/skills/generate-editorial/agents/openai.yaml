interface:
  display_name: "Generate Editorial"
  short_description: "Explicit entry point for editorial page generation."
  default_prompt: "Generate an editorial storefront page."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
