interface:
  display_name: "Generate PDP"
  short_description: "Explicit entry point for product detail page generation."
  default_prompt: "Generate a product detail page for this product."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
