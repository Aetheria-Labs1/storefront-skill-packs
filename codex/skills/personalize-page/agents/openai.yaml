interface:
  display_name: "Personalize Page"
  short_description: "Explicit entry point for persona-specific page variants."
  default_prompt: "Create personalized variants for this page."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
