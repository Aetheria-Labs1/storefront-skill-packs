---
name: setup-brand
description: Configure storefront brand context from approved inputs or a reference URL. Explicit alias for brand setup.
---

# Set Up Brand

Use only when explicitly invoked. Read `../storefront-engine/reference/visual-craft.md`, `design-enrichment.md`, and `design-assets.md`. Use Codex Browser for a reference URL when available, otherwise use Lexsis design extraction; present inferred tokens for confirmation before saving brand settings.
