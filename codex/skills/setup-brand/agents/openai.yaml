interface:
  display_name: "Set Up Brand"
  short_description: "Explicit entry point for storefront brand setup."
  default_prompt: "Set up my storefront brand context."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
