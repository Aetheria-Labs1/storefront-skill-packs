interface:
  display_name: "Generate Homepage"
  short_description: "Explicit entry point for homepage generation."
  default_prompt: "Generate a storefront homepage."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
