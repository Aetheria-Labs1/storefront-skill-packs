interface:
  display_name: "Create A/B Test"
  short_description: "Explicit entry point for storefront A/B testing."
  default_prompt: "Create an A/B test for this page."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
