interface:
  display_name: "Create Assets"
  short_description: "Explicit entry point for storefront visual asset preparation."
  default_prompt: "Create brand-matched visual assets for this storefront."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
