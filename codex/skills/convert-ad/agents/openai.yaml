interface:
  display_name: "Convert Ad"
  short_description: "Explicit entry point for message-matched ad landing pages."
  default_prompt: "Turn this ad into a landing page."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
