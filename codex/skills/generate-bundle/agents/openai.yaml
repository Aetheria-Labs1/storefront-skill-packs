interface:
  display_name: "Generate Bundle"
  short_description: "Explicit entry point for bundle page generation."
  default_prompt: "Generate a bundle page."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
