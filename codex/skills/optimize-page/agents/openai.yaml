interface:
  display_name: "Optimize Page"
  short_description: "Explicit entry point for storefront optimization."
  default_prompt: "Optimize an existing storefront page."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
