interface:
  display_name: "Run Experiment"
  short_description: "Explicit entry point for storefront experimentation."
  default_prompt: "Run a storefront experiment."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
